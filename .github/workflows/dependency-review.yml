# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@3803df9b88945d668bf263edd63ccbacbedbb0b7 # v1.26.0
    permissions:
      contents: read # checkout for the diff of manifests

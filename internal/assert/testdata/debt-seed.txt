# Attestation debt: immutable releases that shipped before an evidence
# obligation existed. audit:attestations reports these and does not fail
# them — the exception you have to write down (#117), same pattern as a
# VEX statement. An entry is repo@tag(asset), one per line; the reason
# rides as a comment. NEVER add a release newer than the obligation it
# is missing: that is a regression, and the audit staying red is the
# point.
#
# In particular, do NOT add a release that went public and then lost its
# verdict to a failed publish run. That is the burned-release state, and
# audit:attestations already reports it WITHOUT an entry here, derived
# from the tag's own run history (#378). Adding it by hand would convert
# a derived fact into an asserted one and hide the next real regression:
# a verdict missing from a release that published cleanly must stay red.
#
# VSA machinery landed in the lab at v0.17.0; earlier releases predate
# the verdict.
release-lab@v0.16.1(attestations-vsa-npm.intoto.jsonl)
release-lab@v0.15.4(attestations-vsa-npm.intoto.jsonl)
release-lab@v0.15.2(attestations-vsa-npm.intoto.jsonl)
release-lab@v0.14.2(attestations-vsa-npm.intoto.jsonl)
# The canon's dotfile-glob skip shipped releases without their SBOM
# asset from v1.0.0 to v1.4.1 — found and fixed in the #106 campaign;
# these are the immutable remainder (v1.0.0/v1.1.0 predate the
# machinery entirely and are reported as legacy instead).
.github@v1.4.1(sbom)
.github@v1.4.0(sbom)
.github@v1.3.2(sbom)
.github@v1.3.1(sbom)
.github@v1.3.0(sbom)
.github@v1.2.2(sbom)
.github@v1.2.1(sbom)
